ext.moduleName = 'cz.honestcity.service'

compileJava {
    inputs.property("moduleName", moduleName)
    doFirst {
        options.compilerArgs = [
                '--module-path', classpath.asPath,
        ]
        classpath = files()
    }
}

dependencies {
    compile project(':model')
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
